import { CONFIG_PORTS } from "."

const {
    PORT, 
    DB_PORT, 
    SMTP_PORT, 
    ACCESS_TOKEN_EXPIRE, 
    REFRESH_TOKEN_EXPIRE
}  = CONFIG_PORTS;

export {
    PORT, 
    DB_PORT, 
    SMTP_PORT, 
    ACCESS_TOKEN_EXPIRE, 
    REFRESH_TOKEN_EXPIRE
}